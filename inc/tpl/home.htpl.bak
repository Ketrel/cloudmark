<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

        <title>{TITLE}</title>

        <!-- Bootstrap core CSS -->
        <link href="./inc/css/bootstrap/4.0.0/bootstrap.min.css" rel="stylesheet">
        <script src="./inc/js/jquery/3.2.1/jquery-min.js" type="text/javascript"></script>

        <!--
            Custom styles for this template
            <link href="starter-template.css" rel="stylesheet">
        -->
    </head>
    <body>
        <div id="nav" class="navbar navbar-toggleable-md navbar-light bg-faded">
            <div class="navbar-brand">{TITLE}</div>
            <div class="navbar-nav">{NAVLINKS}</div>
        </div>
        <br />
        <div id="main" class="container-fluid">
{BREADCRUMBS}
{IF:CATEGORIES}
            <span class="badge badge-default">Categories</span>
            <div class="jumbotron">
                <div class="card-deck" id="categories">
{ENDIF:CATEGORIES}
{IF:CATEGORIES}
{FOREACH:CATEGORIES}
{ENDIF:CATEGORIES}
{IF:CATEGORIES}
                </div>
            </div>
{ENDIF:CATEGORIES}
        </div>
    </body>
</html>
